# 👻 恐鬼症鬼魂特征查看器 · 更新公告

> **当前版本：v2.4**（本次更新覆盖 v2.1.1 → v2.4）

---

## 📌 本次更新总览

本版本在原有「证据三态筛选 + 鬼魂详情 + 悬浮窗」的基础上，带来了 **难度模式、对局进度追踪、候选鉴别建议、可调栏宽、黑夜模式** 五大能力，并修复了悬浮窗恢复与中文排版换行两个问题。

---

## 🆕 新功能

### 🎚️ v2.2 · 难度模式（普通 / 噩梦 / 疯人院 / 零证据）

- **普通**：标准玩法，全部 7 种证据可筛选
- **噩梦**（2 条证据）与 **疯人院**（1 条证据）：
  - 每只鬼有 1 条**强制证据**必定出现在笔记本上，自动套用推理规则：
    御灵→点阵投影仪、幻妖→紫外线、寒魔→刺骨寒温、魔洛伊→通灵盒、雾影→通灵盒、拟魂→灵球
  - 确认满对应数量的证据后，自动排除"强制证据不在其中"的鬼
  - 最多只能确认对应条数（噩梦 2 条、疯人院 1 条），防止误填
  - 详情面板会标注「⭐ 强制证据」
- **零证据**：禁用证据筛选，靠行为特征与识别技巧判断
- 难度切换时自动清空证据筛选

### 🆕 v2.3 · 当前对局进度追踪

- 搜索栏右侧新增 **🆕 开始新对局** 按钮：一键清空搜索、证据筛选与鬼魂选择状态
- 噩梦/疯人院下，状态栏实时显示「已确认证据 X/槽位数」
- 候选只剩 1 只时，状态栏自动标记 **✅ 已锁定**

### 💡 v2.3 · 候选鉴别建议（独立列）

- 候选收窄到 **2–4 只**时，「鬼魂列表」与「鬼魂详情」之间自动出现 **💡 候选鉴别建议** 列
- **🔑 区分线索**：只出现在部分候选身上的证据，标明每个证据对应哪些候选（看到即可锁定/排除）
- **🧪 完整测试方式**：每只候选直接显示完整的识别技巧全文（灯光测试、圣木计时测试等），无需逐个点击
- 候选超过 4 只或不足 2 只时自动隐藏，详情栏自动占满剩余空间

### 📐 v2.3 · 四栏可单独拉宽

- 证据筛选 / 鬼魂列表 / 候选鉴别建议 / 鬼魂详情 之间改为**可拖动分隔条**
- 每栏都能单独拉宽/收窄；列表栏拉宽后按钮网格自动填满
- 候选列宽度变化时自动按新宽度重新排版

### 🌙 v2.4 · 黑夜模式

- 右上角新增「🌙 黑夜模式」开关，一键切换深色界面（证据、列表、详情、候选列全部变暗），夜里玩游戏更护眼
- **Windows 10/11 下系统窗口标题栏也会跟随变为深色**
- 选择保存到配置，下次启动自动恢复
- 切换主题不会清空当前的证据筛选、搜索和查看状态

### 🏷️ 其他

- 窗口标题与界面标题显示**版本号**（v2.4）

---

## 🐛 问题修复

### 修复：按 Esc 收起后再次打开窗口有时仍为缩小后的大小

- **原因**：悬浮模式下重复按 Esc 会覆盖原始窗口几何信息；`overrideredirect` 边框切换是异步的，会把尺寸重置为新内容的请求大小
- **修复**：
  - 悬浮模式下连按 Esc 直接忽略，不再覆盖原始几何
  - 恢复时先让边框切换生效、再应用原始尺寸
  - 边框切换完成后延时兜底再应用一次，确保最终尺寸正确

### 修复：候选完整测试方式的异常换行

- **原因**：`wrap=WORD` 对中英文混排处理很差——中文被当作长词，出现「行首挂逗号」「行尾挂数字」「只剩几个字的碎片行」
- **修复**：改为**中文排版禁则断行**——行首不出闭合标点（，。；：等）、行尾不留开括号（（「等），并支持拖动栏宽后自动重排

---

## ⚙️ 使用说明速览

| 操作 | 说明 |
|------|------|
| 点击证据 | 三态切换：包含（绿）/ 排除（粉）/ 未选（灰） |
| 点击鬼魂 | 三态切换：选中（绿）/ 排除（粉）/ 未选（灰） |
| 顶部难度下拉框 | 切换 普通 / 噩梦 / 疯人院 / 零证据 |
| 搜索框右侧 | 🆕 开始新对局（清空本局状态） |
| 拖动分隔条 | 单独拉宽/收窄四栏 |
| 🌙 黑夜模式 | 切换深色界面（含系统标题栏） |
| Ctrl + F | 聚焦搜索框 |
| Esc | 缩小为 👻 悬浮窗（单击或右键恢复） |
| 顶部标题栏 | 按住拖动窗口 |

> 💡 字号调节、保存字号设置、配置持久化（`%APPDATA%\恐鬼症查看器\config.json`）保持不变，配置新增 `dark` 字段用于黑夜模式。

---

## 📥 获取方式

- 直接运行打包产物 **`dist/恐鬼症查看器.exe`**（单文件，无需安装 Python）
- 或从源码运行：`python src/main.py`

**祝各位调查员猎鬼愉快！👻**
